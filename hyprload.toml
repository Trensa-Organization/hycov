[hyprshell]
description = "window overview for hyprland"
version = "0.1"
author = "Killown"

[hyprshell.build]
output = "build/libhyprshell.so"
steps = [
  "rm -rf build",
  "cmake -DCMAKE_BUILD_TYPE=Debug -B build",
  "cmake --build build"
]

