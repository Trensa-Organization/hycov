[hypershell]
description = "clients overview for hyprland"
version = "0.1"
author = "DreamMaoMao"

[hypershell.build]
output = "build/libhypershell.so"
steps = [
  "rm -rf build",
  "cmake -DCMAKE_BUILD_TYPE=Debug -B build",
  "cmake --build build"
]
